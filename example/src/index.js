import SomeClass from "./example";
import * as Utils from "./utils";
import * as Shapes from "./shapes";
import * as Interfaces from "./interfaces";

/**
 * Example Geometry API Entrypoint
 * @namespace Geometry
 * @summary An example API entrypoint for a geometry package
 */
export default class Geometry {
    static SomeClass = SomeClass;
    static Utils = Utils;
    static Shapes = Shapes;
    static Interfaces = Interfaces;
}