<section>
    <header>
        <?js if (obj.children.length > 0) { ?>
            <ul>
                <?js obj.children.forEach((t) => { ?><li><?js= this.tutoriallink(t.name) ?></li><?js }); ?>
            </ul>
        <?js } ?>
        <h2><?js= obj.header ?></h2>
    </header>
    <article>
        <?js= content ?>
    </article>
</section>
